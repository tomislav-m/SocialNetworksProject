import * as React from 'react';

export default class ErrorPage extends React.Component {
    public render() {
        return (
            <div>
                Error page
            </div>
        );
    }
}